Understand

1. The difference between const and non-const methods

2. How this applies to stl iterators.

  class SomeContainer {
    public:

    const_iterator  begin() const;
    const_iterator  begin() const;
    iterator  end();
    const_iterator  end() const;
    //...
  };

The compiler errors boil down to conversion errors:

/usr/include/c++/4.8/bits/stl_list.h:140:7: 
note:   no known conversion for argument 1 from ‘const std::_List_const_iterator<Node*>’ to ‘std::__detail::_List_node_base*’
